;; =========================
;;   scratch mode
;; =========================

;; scratchの初期メッセージを消す
(setq initial-scratch-message "")
;; scratchのメジャーモードをText-modeにする
(setq initial-major-mode 'text-mode)

